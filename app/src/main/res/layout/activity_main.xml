<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <ImageView
        android:id="@+id/img_camera"
        android:layout_width="0dp"
        android:layout_height="200dp"
        android:text="Gallery"
       android:src="@drawable/default_image"
        android:scaleType="fitCenter"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />
    <Button
        android:id="@+id/btn_find_image"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Find image"
        app:layout_constraintTop_toBottomOf="@id/img_camera"
        app:layout_constraintStart_toStartOf="@id/img_camera"
        app:layout_constraintEnd_toEndOf="@id/img_camera"
        android:layout_marginTop="40dp"
        android:layout_marginEnd="20dp"
        android:layout_marginStart="20dp"
        />
    <Button
        android:id="@+id/btn_predict"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Predict"
        app:layout_constraintTop_toBottomOf="@id/btn_find_image"
        android:layout_marginTop="20dp"
        app:layout_constraintStart_toStartOf="@id/btn_find_image"
        app:layout_constraintEnd_toEndOf="@id/btn_find_image"
        />
    <TextView
        android:id="@+id/tv_imageInformation"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/btn_predict"
        android:layout_marginTop="20dp"
        app:layout_constraintStart_toStartOf="@id/btn_predict"
        app:layout_constraintEnd_toEndOf="@id/btn_predict"

        />

</androidx.constraintlayout.widget.ConstraintLayout>